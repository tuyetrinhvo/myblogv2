{% extends 'base.html.twig' %}

{% block menu %}
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right active">
            <li class="hidden">
                <a href="#page-top"></a>
            </li>
            <li class="active">
                <a href="/">Home</a>
            </li>
            <li class="page-scroll">
                <a href="#portfolio">Articles</a>
            </li>
            <li class="page-scroll">
                <a href="#about">About</a>
            </li>
            <li class="page-scroll">
                <a href="#contact">Contact</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
            <li class="page-scroll">
                <a href="{{ path('user_list') }}">Users</a>
            </li>
            {% endif %}
        </ul>
    </div>
{% endblock %}

{% block body %}
<section id="portfolio">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2>Liste des Articles</h2>
                <hr class="star-primary">
            </div>
        </div>

        <div class="row">
        {% for article in articles %}
            <div class="col-sm-4 col-lg-4 col-md-4">
                <div class="caption">
                    <h4><a href="{{ path('article_show', {'id' : article.id }) }}">{{ article.title }}</a></h4>
                    <p><em>Auteur : {{ article.author.username }}</em></p>
                    <p>{{ article.content }}</p>
                </div>
                <div>
                    <form action="{{ path('article_delete', {'id' : article.id }) }}">
                        <button class="btn btn-danger btn-sm pull-right">Supprimer</button>
                    </form>
                </div>
                <div>
                    <form action="{{ path('article_edit', {'id' : article.id }) }}">
                        <button class="btn btn-danger btn-sm pull-right">Modifier</button>
                    </form>
                </div>
            </div>
            <div class="alert alert-warning" role="alert">
                Il n'y a pas encore de l'article enregistré. <a href="{{ path('article_create') }}" class="btn btn-warning pull-right">Créer un article</a>
            </div>
        {% endfor %}

        </div>
    </div>
</section>

{% endblock %}
